package liadinomullen;

public class GetCurrentChange {
	
	
	//subtract purchase price from payment 
	//if if modulus to the first value != 0 then move the smaller value
	public static void getChange() {
	for(int i = 0; i<8; i++) {
		if(nums[i].getValue1 % IntroQs.change != 0)
	}
	}

}
